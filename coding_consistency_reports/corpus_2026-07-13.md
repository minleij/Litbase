# Coding Consistency Report

**Scope:** Whole corpus (31 records)
**Compared:** Working tree vs. HEAD (uncommitted recodes), except `patel_2022_multi_human_multi_robot_remote_interaction` which compares its two most recent commits (its recode was already committed)
**Generated:** 2026-07-13

## Summary

- Articles compared: 11 (20 skipped — only one committed version exists yet, nothing to compare)
- Drift needing review: **0 items**
- Version bumped, undocumented in notes: **0 items**
- Expected changes (documented): 11 classification code changes + 2 study-type-justification revisions, all across 11 articles
- Notable text/substance changes: 0 beyond the documented study-type edge-case revisions (see below)
- Newly-opened items (not drift): 1 (`yang_2025`, a new flag for a portion of a dimension not resolved by this recode)

**Bottom line: no inconsistency found.** Every classification code that changed did so under a codebook version bump, and every one of those changes is explained in the record's own `extraction_metadata.notes`. This recode pass looks clean.

---

## bashyal_2008_radiation_source_hsi

**Codebook versions:** `task_environment`: 0.9 → 0.10 (bumped) · `study_type`: 0.3 → 0.3 (unchanged)

### Expected changes (documented)
- **behavioral_relation_to_task_object** (Experiment A): `Unclear / Candidate New Code` → `Move Task` — per note: "Re-coded behavioral_relation_to_task_object for Experiment A again ... using the newly-added Move Task code (v0.10) ... which this article's own flag had proposed."

### Flags
- Removed: 1 (the `Unclear / Candidate New Code` flag this recode resolved) — already explained above.

No drift, no undocumented version-bump changes.

---

## cummings_2008_mauve_controller_capacity

**Codebook versions:** `task_environment`: 0.9 → 0.10 (bumped) · `study_type`: 0.3 → 0.3 (unchanged)

### Expected changes (documented)
- **computational_characteristics** (active-LOA decision-support scheduling algorithm): `Unclear / Candidate New Code` → `Not Applicable` — per note: "Re-coded computational_characteristics again ... per the new operator-directed-computation scope note (v0.10) ... per the user's decision not to add a dedicated code for this pattern."

### Flags
- Added 1 / removed 1 — same item, the `Unclear` flag replaced by a `Not Applicable` flag as the dimension was resolved. Already explained above.

No drift, no undocumented version-bump changes.

---

## donmez_2010_des_workload_uv

**Codebook versions:** `study_type`: 0.2 → 0.4 (bumped) · `task_environment`: 0.8 → 0.8 (unchanged)

No classification code changes this time (task_environment untouched, as expected since its version didn't move).

### Expected changes (documented)
- **study_type.justification**: substantially revised — per note: "Re-coded study_type Secondary again ... per the new Section 3.11 edge-case note (Study-Type codebook v0.4): the DES workload model is confirmed as Modeling/Theoretical Paper (Secondary), accepted as an edge case." The Secondary *code* itself (`Modeling/Theoretical Paper`) didn't change — only the justification was rewritten to reflect the newly-accepted edge-case reasoning, replacing what used to be an open caveat. This is a substantive but fully-documented change, not drift.

### Flags
- Removed: the `Unclear / Candidate New Code` flag on `study_type` — resolved by the same v0.4 edge-case note above.

No drift.

---

## fortmann_2013_supervisory_guide_uav_swarm

**Codebook versions:** `task_environment`: 0.8 → 0.10 (bumped) · `study_type`: 0.3 → 0.3 (unchanged)

### Expected changes (documented)
- **computational_characteristics** (Supervisory Guide's own SA-gap-detection/guidance mechanism): `Unclear / Candidate New Code` → `Not Applicable` — per note: "Re-coded computational_characteristics on 2026-07-13 per the new operator-directed-computation scope note (v0.10) ... per the user's decision not to add a dedicated code for this pattern."

### Still open (unchanged, correctly left alone)
- **behavioral_relation_to_task_object** (intruder classification/deconfliction): still flagged `Unclear / Candidate New Code`. The note text was updated to reflect that the new Move Task code doesn't resolve it either, but the note explicitly says "Not addressed in the 2026-07-13 codebook update" — this is a wording refresh on a still-open item, not drift.

### Flags
- Added 1 / removed 1 for the resolved `computational_characteristics` item (explained above). One flag's note text updated only (the still-open intruder-classification item) — expected, not drift.

No drift.

---

## frische_2013_sa_tracer_uav_swarm

**Codebook versions:** `task_environment`: 0.8 → 0.10 (bumped) · `study_type`: 0.3 → 0.3 (unchanged)

### Expected changes (documented)
- **computational_characteristics** (SA-Tracer's own SA-inference mechanism): `Unclear / Candidate New Code` → `Not Applicable` — per note: "Re-coded computational_characteristics on 2026-07-13 per the new operator-directed-computation scope note (v0.10) ... per the user's decision not to add a dedicated code for this pattern." Same pattern as `cummings_2008` and `fortmann_2013` above — this operator-directed-computation resolution was applied consistently across all three articles that shared the gap.

No drift.

---

## naghsh_2008_guardians_firefighting_swarm

**Codebook versions:** `task_environment`: 0.8 → 0.10 (bumped) · `study_type`: 0.3 → 0.3 (unchanged)

### Expected changes (documented)
- **behavioral_relation_to_task_object** (exit/scene-of-incident direction guidance): `Unclear / Candidate New Code` → `Move Task` — per note: "Re-coded behavioral_relation_to_task_object (exit/scene-of-incident direction guidance) ... using the newly-added Move Task code (v0.10)."

No drift.

---

## parasuraman_2005_roboflag_delegation

**Codebook versions:** `study_type`: 0.2 → 0.4 (bumped) · `task_environment`: 0.8 → 0.8 (unchanged)

No classification code changes this time (task_environment untouched, as expected).

### Expected changes (documented)
- **study_type.justification**: revised — per note: "Re-coded study_type Secondary again ... the Section V workload model is confirmed as Modeling/Theoretical Paper (Secondary), accepted as an edge case." Same v0.4 edge-case pattern as `donmez_2010` above, applied consistently to the second article that shared the same borderline fit. The Secondary code itself is unchanged.

### Flags
- Removed: the `Unclear / Candidate New Code` flag on `study_type` — resolved by the same v0.4 note.

No drift.

---

## patel_2022_multi_human_multi_robot_remote_interaction

*(This one compares the project's two earliest commits, since its recode was already committed rather than sitting in the working tree — it's included here as confirmation the comparison logic still finds it correctly.)*

**Codebook versions:** `task_environment`: 0.8 → 0.9 (bumped) · `study_type`: 0.3 → 0.3 (unchanged)

### Expected changes (documented)
- **behavioral_relation_to_task_object**: `Unclear / Candidate New Code` → `Transport/Delivery Task` — per note: "using the newly-added ... Transport/Delivery Task codes (v0.9), which this article's own flags had proposed (the latter originating here)."
- **task_object_spatiotemporal_dynamics**: `Unclear / Candidate New Code` → `Known Fixed Task Object` — same note, same v0.9 bump.

No drift.

---

## turco_2024_shared_control_swarm_teleoperation

**Codebook versions:** `task_environment`: 0.8 → 0.10 (bumped) · `study_type`: 0.3 → 0.3 (unchanged)

### Expected changes (documented)
- **operational_domain**: `Unclear / Candidate New Code` → `Confined/Enclosed Structure` — per note: "using the newly-added Confined/Enclosed Structure code (v0.10), which this article's own flag had proposed (and is the code's own source example)."

No drift.

---

## villani_2020_affect_based_multi_robot_interaction

**Codebook versions:** `task_environment`: 0.9 → 0.10 (bumped) · `study_type`: 0.3 → 0.3 (unchanged)

### Expected changes (documented)
- **behavioral_relation_to_task_object** (Experiments 1–2, physical e-puck robots): `Unclear / Candidate New Code` → `Move Task` — per note: "Re-coded behavioral_relation_to_task_object for Experiments 1-2 again ... using the newly-added Move Task code (v0.10) ... the companion task_object_spatiotemporal_dynamics Unclear flag for the same experiments was already resolved."

No drift.

---

## yang_2025_mmf_microrobotic_swarm_teleoperation

**Codebook versions:** `task_environment`: 0.9 → 0.10 (bumped) · `study_type`: 0.3 → 0.3 (unchanged)

### Expected changes (documented)
- **behavioral_relation_to_task_object** (navigate-to-target portion): `Unclear / Candidate New Code` → `Move Task` — per note, using the v0.10 Move Task code.
- **operational_domain**: `Unclear / Candidate New Code` → `Confined/Enclosed Structure` — per note, using the v0.10 code.

### Newly-opened item (not drift)
- **behavioral_relation_to_task_object** (formation/cohesion-maintenance portion): a *new* `Unclear / Candidate New Code` flag was opened for the part of the original flag that Move Task doesn't cover. The note is explicit about this: "The formation/cohesion-maintenance portion ... is not resolved by either new code and remains open as its own flag entry." This is a deliberate split of one open question into a resolved part and a still-open part — not an inconsistency.

No drift.

---

## Skipped articles

Only one committed version exists yet for these — nothing to compare until they're recoded a second time:

`burkle_2011_micro_uav_swarm_platform`, `digregorio_2021_gcs_team_sa`, `divband_soorati_2021_user_centered_interface`, `fern_2018_momu_playbook`, `gale_2018_playbook_casas`, `hughes_2012_muscit_multi_uav`, `jia_2026_multi_uav_sa_llm`, `jo_2025_awac_mhmr_teams`, `kerman_2012_stakeholder_swarm_leadership`, `kira_2009_human_control_decentralized_swarms`, `leijonhufvud_2026_mhc_drone_swarms`, `lewis_2013_multi_uv_hri_review`, `osga_2013_mocu_usv_hci`, `paas_2022_swarm_control_cognitive_load`, `peschel_2013_uas_mission_specialist_survey`, `ramchurn_2015_maxsum_uav_task_allocation`, `roundtree_2021_collective_visualization_transparency`, `taylor_2017_delcon_mum_t`, `wattearachchi_2025_hsi_task_performance`, `williamson_2023_command_control_swarm`, `zhou_2025_ooda_uav_swarm_hsi`
